# Christmas Wishlist (Projeto em Python)

Aplicativo web simples para colecionar desejos de Natal. Usa Flask e SQLite.

## Requisitos
- Python 3.8+
- pip

## Instalação e execução local
1. Crie e ative um virtualenv (recomendado):
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # Linux / macOS
   .venv\Scripts\activate      # Windows (PowerShell)
